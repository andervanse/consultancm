
var status = {
    RUNNING: 'RUNNING',
    PAUSED: 'PAUSED',
    ENDED: 'ENDED'
}

var timeDirection = {
    FORWARD: 'FORWARD',
    BACKWARD: 'BACKWARD'
}

export { status, timeDirection }